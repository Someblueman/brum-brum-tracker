# Home location coordinates
HOME_LAT=your_latitude_here
HOME_LON=your_longitude_here

# OpenSky Network OAuth2 credentials (optional but recommended)
# Get free account at https://opensky-network.org/
# For new accounts (created after March 2025), use OAuth2:
# 1. Log in to your OpenSky account
# 2. Go to Account settings
# 3. Create a new API client
# 4. Use the client_id as OPENSKY_USERNAME
# 5. Use the client_secret as OPENSKY_PASSWORD
OPENSKY_USERNAME=your_client_id_here
OPENSKY_PASSWORD=your_client_secret_here

# Authentication settings (for production deployment)
# Set AUTH_ENABLED=true to enable authentication
AUTH_ENABLED=false
AUTH_USERNAME=admin
AUTH_PASSWORD=your_secure_password_here
# AUTH_SECRET is auto-generated if not provided
# AUTH_TOKEN_EXPIRY=3600  # Token expiry in seconds (default: 1 hour)